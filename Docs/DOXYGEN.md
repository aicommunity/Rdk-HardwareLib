# Doxygen для Rdk-HardwareLib

## RU

## Роль

| Источник | Назначение |
|----------|------------|
| `Docs/*.md` | Руководство: компоненты, протокол, GUI, примеры |
| Doxygen (`Core/`) | Справочник по C++ API из `.h` |

Markdown **не** генерируется Doxygen автоматически; не дублируйте длинные описания в комментариях к классам без необходимости.

## Запуск

```bash
cd Libraries/Rdk-HardwareLib/Docs/Doxygen
doxygen Doxyfile
```

Результат: `Docs/Doxygen/html/index.html`.

## INPUT

В `Doxyfile`: `INPUT = ./../../` — корень библиотеки (`Core/`, `GUI/`, `Firmware/`). При необходимости сузить до `../../Core` для более быстрой сборки.

## Согласованность

- ClassName и свойства в `Docs/Components/` совпадают с `Bin/ClDesc/HardwareLibrary/ru-RU/*.xml` и `Docs/ClDescLexicon.json`.
- Тестовые конфиги: `Bin/Configs/SpikeSamples/Hardware/README.md`.

---

## EN

## Role

| Source | Purpose |
|--------|---------|
| `Docs/*.md` | Guide: components, protocol, GUI, examples |
| Doxygen (`Core/`) | C++ API reference from `.h` |

Markdown is **not** generated by Doxygen automatically; do not duplicate long descriptions in class comments unless necessary.

## Run

```bash
cd Libraries/Rdk-HardwareLib/Docs/Doxygen
doxygen Doxyfile
```

Output: `Docs/Doxygen/html/index.html`.

## INPUT

In `Doxyfile`: `INPUT = ./../../` — library root (`Core/`, `GUI/`, `Firmware/`). Narrow to `../../Core` if needed for faster builds.

## Consistency

- ClassName and properties in `Docs/Components/` match `Bin/ClDesc/HardwareLibrary/ru-RU/*.xml` and `Docs/ClDescLexicon.json`.
- Test configs: `Bin/Configs/SpikeSamples/Hardware/README.md`.
